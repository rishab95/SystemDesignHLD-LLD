package Coding.Salesforce;

/**
 * The developers of Hackerland want to construct a network of servers across the country to
 * scale their applications. The country of Hackerland can be represented as a large coordinate
 * grid and the developers have installed n servers across the country.
 * The ith server is located at coordinate (x], yl).
 * The cost of connecting any two servers indexed i and jis min(/ x[i] - x/]/, (y- y]/)where /al
 * represents the absolute value of an integer a.
 * Given the arrays xand y of n integers each, find the minimum cost of constructing the network such that every server is reachable by every other server via some
 */
public class MST {
}
